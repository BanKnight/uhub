{
  "stories": [
    {
      "id": "AGG-US1",
      "title": "为 admin structured channel contract 新增稳定聚合入口",
      "priority": 1,
      "passes": true,
      "acceptanceCriteria": [
        "`package.json` 新增 `test:integration:admin`，只聚合现有 `test:integration:admin:channel-structured` 与 `test:integration:admin:channel-structured:invalid` 两条脚本",
        "`package.json` 新增 `test:integration:admin:local`，并继续通过 `tests/run-with-local-worker.ts` 包装 `test:integration:admin`",
        "本轮改动不扩大默认 smoke 集合，不修改 runtime 业务代码，也不改已有测试逻辑"
      ]
    },
    {
      "id": "AGG-US2",
      "title": "验证 admin 聚合入口并记录结果",
      "priority": 2,
      "passes": true,
      "acceptanceCriteria": [
        "运行 `bun run --cwd /data/workspace/uhub test:integration:admin:local` 通过",
        "`.omc/progress.txt` 追加记录本轮 admin 聚合入口的实现、验证结果与 learnings",
        "git working tree 中仅包含本轮聚合入口相关的 `package.json` / `.omc` 元数据改动，不残留已放弃方案噪音"
      ]
    }
  ]
}
